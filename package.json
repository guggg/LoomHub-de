{
  "name": "loomhub-de",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Team skill-sharing hub — tooling (Stage 2: index builder).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build-index": "node scripts/build-index.mjs",
    "install-skill": "node scripts/install-skill.mjs",
    "test:scripts": "vitest run",
    "test:site": "npm --prefix site run test",
    "test": "npm run test:scripts && npm run test:site"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
